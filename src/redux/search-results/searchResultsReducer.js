const initialState = {
	searchResults: []
};

const searchResultsReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default searchResultsReducer;
